# Staging reverse proxy. {$SITE_ADDRESS} is read from the container env
# (set SITE_ADDRESS in apps/api/.env, e.g. staging.verity.example.com).
# Caddy obtains and renews a Let's Encrypt cert automatically for that domain.
{$SITE_ADDRESS} {
	reverse_proxy api:3000
}
